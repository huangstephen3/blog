---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_LANG, SITE_TITLE, STARTER_STEPS } from '../consts';
---

<!doctype html>
<html lang={SITE_LANG}>
	<head>
		<BaseHead title={`关于本站 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<style>
			.about-grid {
				display: grid;
				grid-template-columns: minmax(0, 1fr) 320px;
				gap: 1rem;
			}

			.panel {
				padding: 1.2rem;
				border-radius: 20px;
				background: rgb(var(--panel));
				border: 1px solid rgba(var(--line), 1);
			}

			.step-list {
				margin: 0;
				padding-left: 1.2rem;
			}

			.step-list li + li {
				margin-top: 0.7rem;
			}

			@media (max-width: 900px) {
				.about-grid {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<div class="about-grid">
				<section class="panel">
					<p class="eyebrow">About The Desk</p>
					<h1>这是一个独立写作者的 AI 新闻编辑室</h1>
					<p>
						这个站点已经不再是模板式博客，而是你自己的 AI 科技媒体品牌。它会持续追踪模型发布、
						产品更新、创业公司、芯片、Agent、政策和产业落地。
					</p>
					<p>
						我保留了轻量的技术栈，是为了让你把注意力放在写作和选题，而不是后台维护。现在站点已经具备
						外网访问、自动部署和读者登录入口，接下来最重要的是坚持更新内容。
					</p>
					<p>
						如果你后续想把它做成更完整的 AI 新闻产品，我们还可以继续加入邮件简报、私密栏目、专题页、
						评论区、资料库和会员订阅体系。
					</p>
				</section>

				<aside class="panel">
					<p class="eyebrow">Editorial Loop</p>
					<h2>你接下来最重要的事</h2>
					<ol class="step-list">
						{STARTER_STEPS.map((step) => <li>{step}</li>)}
					</ol>
				</aside>
			</div>
		</main>
		<Footer />
	</body>
</html>
