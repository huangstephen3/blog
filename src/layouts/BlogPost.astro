---
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';
import { DEFAULT_LOCALE, getLocale, getMessages } from '../lib/i18n';

type Props = CollectionEntry<'blog'>['data'];

const { title, description, pubDate, updatedDate, heroImage, tags = [], category, premium, locale = DEFAULT_LOCALE } = Astro.props;
const messages = getMessages(locale);
const localeMeta = getLocale(locale);
---

<html lang={localeMeta.lang}>
	<head>
		<BaseHead title={title} description={description} image={heroImage} />
		<style>
			.post-shell {
				display: grid;
				grid-template-columns: minmax(0, 1fr) 300px;
				gap: 1rem;
			}

			.post-main,
			.post-side {
				background: rgb(var(--panel));
				border: 1px solid rgba(var(--line), 1);
				border-radius: 20px;
			}

			.post-main {
				padding: 1.1rem;
			}

			.post-side {
				padding: 1.2rem;
				height: fit-content;
			}

			.hero {
				margin-bottom: 1.2rem;
			}

			.hero img {
				display: block;
				width: 100%;
				border-radius: 16px;
			}

			.meta {
				display: flex;
				flex-wrap: wrap;
				gap: 0.55rem 0.9rem;
				color: rgb(var(--muted));
				font-size: 0.92rem;
			}

			h1 {
				margin: 0.7rem 0 0.8rem;
				font-size: clamp(1.65rem, 3.6vw, 2.35rem);
			}

			.desc {
				margin: 0 0 1rem;
				color: rgb(var(--muted));
			}

			.badges {
				display: flex;
				flex-wrap: wrap;
				gap: 0.5rem;
				margin-bottom: 1rem;
			}

			.badge {
				padding: 0.28rem 0.72rem;
				border-radius: 999px;
				background: rgba(var(--accent), 0.09);
				color: rgb(var(--accent));
				font-size: 0.88rem;
			}

			.prose {
				color: rgb(var(--ink));
			}

			.prose :global(p) {
				margin: 0 0 1rem;
			}

			.side-title {
				margin: 0 0 0.8rem;
				font-size: 1rem;
			}

			.side-list {
				margin: 0;
				padding-left: 1rem;
				color: rgb(var(--muted));
			}

			.side-list li + li {
				margin-top: 0.6rem;
			}

			@media (max-width: 900px) {
				.post-shell {
					grid-template-columns: 1fr;
				}
			}
		</style>
	</head>
	<body>
		<Header locale={locale} />
		<main>
			<div class="post-shell">
				<article class="post-main">
					{heroImage && (
						<div class="hero">
							<Image width={1280} height={720} src={heroImage} alt={title} />
						</div>
					)}
					<div class="meta">
						<span>{category}</span>
						<span><FormattedDate date={pubDate} locale={localeMeta.lang} /></span>
						{updatedDate && <span>{messages.updated} <FormattedDate date={updatedDate} locale={localeMeta.lang} /></span>}
					</div>
					<h1>{title}</h1>
					<p class="desc">{description}</p>
					<div class="badges">
						{premium && <span class="badge">Subscriber Preview</span>}
						{tags.map((tag: string) => <span class="badge">#{tag}</span>)}
					</div>
					<div class="prose">
						<slot />
					</div>
				</article>

				<aside class="post-side">
					<h2 class="side-title">{messages.signalNotes}</h2>
					<ul class="side-list">
						{messages.signalNoteItems.map((item) => <li>{item}</li>)}
					</ul>
				</aside>
			</div>
		</main>
		<Footer locale={locale} />
	</body>
</html>
